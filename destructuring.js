const fish = {
    name: 'hilsha',
    address: 'borishal',
    color: 'silvar',
    price: 5000
}
console.log(fish.address);

const {name, address, color, price} = fish;
console.log(address, name, price);